# Optimization Tracking Log

Each row = one hourly cycle. Format: `[timestamp] item-id: description of change`

---

2026-08-31T19:32:09Z — optimization trigger sent (74 items remaining)
2026-08-31T20:12:56Z — optimization trigger sent (74 items remaining)
2026-08-31T20:14:16Z — optimizer trigger sent (74 items remaining)
2026-08-31T20:22:00Z — #54 numbers-consistent: fixed stale "8 ad spaces" → "31 identical ad squares" in definition/og/twitter meta descriptions on index.html (was stale since v22 uniform-grid change). Verified no other stale 8-spot refs; all pages 200, app.js compiles.
2026-08-31T21:20:00Z — #45 robots.txt: created robots.txt (Allow all + Sitemap ref). Was missing entirely despite sitemap.xml existing. Improves crawlability/SEO. Verify live serves 200.
2026-08-31T22:20:00Z — #23 dns-prefetch: added <link rel="preconnect" crossorigin> + <link rel="dns-prefetch"> for Cloudflare Worker API (notghostingyou-api.vanshkhanna416.workers.dev) in index.html <head>. Warms TLS/DNS for first API call. (Stripe loads dynamically via jsdelivr in app.js; primary predictable origin is the API.)
2026-08-31T23:20:00Z — #35 skip-link: added accessible "Skip to content" link as first element in <body> targeting #main (hero section). Added .skip-link CSS: off-screen until :focus, then visible top-left, keyboard-navigable. Improves a11y for keyboard/screen-reader users.
2026-09-01T00:20:00Z — #33 aria-live: added aria-live="polite" to dynamic auction elements (heroRaised, progressLabel, stretchNote) in index.html. These update via poller as bids come in; screen readers now announce changes. (ticker + countdown already had aria-live/timer.)
2026-09-01T01:20:00Z — #15 broken-links/404: verified all internal anchors+links resolve (index#spots/faq/how/specs/main + all .html exist; ownerLink JS-populated). Added branded 404.html (git-pages default was showing GitHub's generic 404). Includes noindex, site header/footer, links back to auction/marketplace. Soft-404 keeps visitors on-site.
2026-09-01T02:20:00Z — #58 press fact-sheet: fixed stale "8 lid spots" → "31 identical 4×4cm lid squares" in press.html (meta description, og:description, fact-sheet 'What' line). Was stale since v22 uniform-grid change. Full sweep confirms no other stale 8-spot refs anywhere; all pages 200.
2026-09-01T03:20:00Z — #57 terms-numbers: fixed stale "8 ad spots" → "31 identical 4×4cm ad squares" in terms.html (The auction clause). Comprehensive sweep now confirms ZERO stale 8-spot refs, zero stale device/price refs (M5 Max/36GB/7099/3760/10517), zero premium-tier language across all pages. v22 grid consistency complete.
2026-09-01T04:20:00Z — #76 secrets: verified no secrets in client-side code. Frontend exposes only the publishable pk_test Stripe key (correct); no sk_/sk_live, no auth tokens/API keys. Backend reads STRIPE_SECRET_KEY + AUTH_TOKEN from Worker env bindings (via `npx wrangler secret put`), NOT committed; wrangler.toml has only comments. Also verified #77 (key is publishable not secret) and #78 (all API calls HTTPS via apiBase). Security posture clean — verified, no code change required.
2026-09-01T05:20:00Z — #71 legend: fixed stale legend label. With the v22 uniform grid all spots are size "S", but the legend still labeled them "Pad" (leftover from old premium-tier naming). Changed sizeLabel S: "Pad" → "Square" in app.js. Legend now correct: "Square · from A$140". JS compiles, verified render logic.
2026-09-01T06:20:00Z — #93 areaOf: verified all 31 spots are type:"square", w:4, h:4 (4×4cm) with areaOf.square=16 (16cm²) — geometrically correct. Cross-checked money-critical logic: #84 min-bid = current+10 (A$10 increment) else spot.price (correct); #85 deposit = 20% of bid via DEPOSIT_PCT (correct); #92 target display from DATA.spots[].target (correct). Business + geometry logic verified clean, no code change required.
2026-09-01T07:20:00Z — #68 countdown: polished unit formatting. Verified timer correct (29d to 2026-09-30, handles closure at diff<=0), but final minute showed awkward leading "0m" (e.g. "0m 45s"). Changed to omit zero units: hours only if nonzero, minutes only if nonzero. Now clean "45s" in the final minute, "1h 0s" in final hour. JS compiles; tested NOW/edge/boundary cases.
2026-09-01T08:20:00Z — #41 meta-description length: index.html meta+og description was 184 chars, press.html 186 chars — both exceeded the ~160-char SERP truncation limit. Tightened: index → 140 (both meta and og), press meta → 152. Verified all 6 pages now have titles ≤60 and meta descriptions ≤153. Couldn't fit a canonical/structured-data item this cycle; left for a future cycle.
2026-09-01T09:20:00Z — #47 structured data: JSON-LD in index.html was stale since v22. Description said "8 lid sticker spots"; offers were A$550–1500 with old Offer type (no count). Updated to accurate v22: AggregateOffer A$140–337 (per-spot price/target), offerCount 31, description "31 identical 4x4cm lid squares". Validated JSON parses. Prevents Google rich results showing wrong pricing/count. Couldn't fit canonical <link> check this cycle; #46 confirmed present on all pages earlier.
2026-09-01T10:20:00Z — #42 og:image: press/terms/privacy/marketplace lacked og:image (no preview image when shared). Added canonical og-image.png to all four. Also fixed stale og:image:alt + twitter:image:alt on index ("pre-order a sticker spot" → "31 lid squares"). Verified all 5 pages now have og:image; JSON-LD still valid.
2026-09-01T11:20:00Z — #52 hero copy: reviewed hero for <5s clarity; H1 strong ("Put your brand on the laptop I carry everywhere"). Tightened lede — it repeated "31" three times and restated same-size/same-price after already saying "identical". Now reads: "31 identical sponsor squares … Same size, same price — pick any open spot." Cleaner flow, trust point preserved. Also verified this cycle: #28/#31 (icon buttons + form labels all present), #6 (no dead code/console.log), #12 (all img have alt), #22 (CSS in head is standard; JS at body end).
2026-09-01T12:20:00Z — #67 theme persistence: verified dark mode persists across reloads (THEME_KEY blt_theme_v1 + localStorage, in-memory fallback for file://). Fixed a real UX bug: data-theme="light" is hardcoded on <html> and app.js (end-of-body) applied the saved theme late → flash-of-light for dark users on every reload. Added tiny inline head script that reads blt_theme_v1 and sets data-theme before first paint. Only index has the toggle; other pages stay light by design. Page serves 200.
2026-09-01T13:20:00Z — #81 XSS vectors: audited all bidder-supplied-data render sites. Bidder names/URLs were escaped (escapeHtml covers & < > " '), setAttribute/textContent used elsewhere. FOUND stored-XSS vector: live-mode lid grid injected user-uploaded logo (from /logo/:spotId, raw file) UNESCAPED into <img src="..."> (app.js line 161) — a bidder-uploaded logo containing `" onerror=...` could break out of the attribute. Fixed with escapeHtml(b.logo). Verified: legit base64 data URIs unchanged (no quotes in base64), malicious payload neutralized. app.js compiles.
2026-09-01T14:20:00Z — #64 mobile grid: found a v22 uniform-grid regression — every square is w:4, so `.spot.tiny` (w<=4) applied to ALL 31 spots and `.spot.tiny .name {display:none}` hid every A1–E8 label on the grid, even on desktop. Fixed: labels now show ≥480px (squares ~49px+, "A1" fits), hidden only on narrow mobile (375px squares ~37px). Grid↔list matching restored on desktop/tablet. Also verified this cycle: #56 FAQ answers all accurate against config, #73 carousel swipe works (touch+pointer guarded), #65 hover states present, #7 data.js already strict, #48 titles unique. CSS balanced, pages 200.
2026-09-01T15:20:00Z — #61 how-it-works band: three steps were clear and accurate except step 2 ("I'll reach out to arrange payment") glossed over the real flow. Aligned with terms/FAQ: "Your 20% deposit counts toward the price; if you win I invoice the remainder." Also verified #79 no inline event handlers (only two harmless inline style="" on 404.html). Pages 200.
2026-09-01T16:20:00Z — #60 marketplace waitlist: flow was clear (vision copy + email form + API POST + feedback), but BOTH waitlist handlers (marketplace.html and index via app.js) on network failure said "Saved — we'll confirm by email" in green — a false positive: the email was NOT saved. Changed failure path to honest red "Couldn't reach the server — try again in a moment." Local demo path (live=false) still saves in-memory with green. Also verified #8 structural checks (no duplicate IDs, all tags balanced). JS compiles.
2026-09-01T17:20:00Z — #59 privacy completeness: page already disclosed collected fields, use, no-sale/no-trackers, storage+deletion, local prefs. Gap: the site takes live card deposits via Stripe but privacy page never disclosed a third-party payment processor. Added a Payments bullet: deposits via Stripe, card details never seen/stored by the site, Stripe's policy applies. Material completeness fix for a live-payment site.
2026-09-01T18:20:00Z — #53 typos/grammar: swept all 6 HTML pages for common misspellings, double words (the the/is is), stray spaces before punctuation, straight-quote misuse, mojibake, and unclosed img tags — clean, no errors. Confirmed prose punctuation is clean. Was a verification pass (no code change). Also formalized #8 W3C/nesting (previously verified: no duplicate IDs, all tags balanced) as done on the checklist.
2026-09-01T19:20:00Z — #34 focus indicators: only .skip-link had focus styles; no :focus-visible anywhere, so keyboard users relied on default (often invisible) focus. Added a global :focus-visible rule (3px accent outline, 2px offset) covering every interactive element, plus a tuned rule for the skip-link. CSS balanced, pages 200.
2026-09-01T20:20:00Z — #87 refunds: audited the money flow. /deposit charges 20% via Stripe Checkout and /confirm activates the bid on ?deposit=ok return, but NO refund mechanism exists in the worker — no PaymentIntent capture, no /refund endpoint, no scheduler. Copy promised "Refunded in full if outbid" (Stripe receipt line-item, bid-modal note, FAQ, terms). DECISION (god choice): harden the copy instead of wiring Stripe refund automation. Copy now truthfully states the founder refunds deposits in full BY HAND if outbid / floor not met / bid refused — on worker/src/index.js:158 (Stripe receipt), js/app.js bid-modal note, index.html FAQ, terms.html Deposits clause. Promise intact; mechanism manual. Tracked: automated refunds would need a Stripe webhook + PaymentIntent capture + /refund endpoint + D1 columns — deferred, founder-owned.
2026-09-01T22:20:00Z — #90 bid submission E2E: hit the LIVE worker (read-only + validation-only, no data written): /health 200 ok; /bids 200 {}; POST /bid with missing fields → 400 "Missing required fields." (validation path works). Full bid→/confirm→/deposit flow was already code-verified in cycles #84/#85/#93 (min-bid guard, 20% deposit math, bidder token). A live POST was not done to avoid polluting the real auction data. E2E wiring confirmed healthy.
2026-09-01T23:20:00Z — #70 spot list on mobile: .spot-list is a natural flex column (page scrolls normally, no fixed-height scroll trap); each .sl-row uses flex-wrap with min-width fallbacks (.sl-spot min-width 200px, .sl-bid min-width 100px, cta flex:none), so rows wrap gracefully on narrow screens with no horizontal overflow. Lid scales via width:min(620px,92vw) + aspect-ratio (already verified at 375px in #64). No code change — behavior verified clean.
2026-09-02T00:20:00Z — #4/#7/#11 JS hygiene: no console.log anywhere; "use strict" present in app.js + data.js; only explanatory comments (no dead/commented code blocks). Verified clean.
2026-09-02T01:20:00Z — #12/#13/#14 HTML fundamentals: no empty alt attributes; lang="en" on all 6 pages; UTF-8 charset + viewport on all 6 pages. Verified clean.
2026-09-02T02:20:00Z — #66 bid modal mobile FIX: .modal was flex-centered with no overflow-y:auto — on short screens the card (tall: name/email/url/logo/amount + agree) overflowed the viewport and the top (title, close button) was unreachable. Added overflow-y:auto + -webkit-overflow-scrolling:touch to .modal and margin:auto on .modal-card so it centers when it fits and scrolls from the top when it doesn't. CSS balanced.
2026-09-02T03:20:00Z — #86 outbid notification: logic verified correct. Worker fetches incumbent email BEFORE the upsert (worker/src/index.js:72), then fires sendOutbidEmail only when a different email outbids (guard at :93). Caveat: only sends if founder sets optional RESEND_API_KEY. Owner notify likewise gated. No code change.
2026-09-02T03:50:00Z — #55/#28/#30: CTAs all action-oriented ("Get a spot", "Join the waitlist", "Back to the live auction"); icon-only buttons have aria-label (theme toggle, carousel prev/next, modal close); custom interactive regions carry role (region/timer/group). Verified clean.
2026-09-02T05:20:00Z — #29 contrast + #31 labels + #84 min-bid: (a) WCAG contrast audit across both themes — all pairs AA PASS except light --red #d93b3b at 3.96:1 on bg-soft (used for small error text) → darkened to #c23030 (5.21:1 on bg-soft). (b) marketplace waitlist email input had no label → added aria-label="Email address" (bid-modal inputs already use wrapping <label>). (c) FOUND REAL BUG: server min-bid for an unbidden spot was 100 (worker/src/index.js:67) while the frontend enforces spot.price (140) — a crafted client could take a spot below list price via the API. Fixed: floor = Number(env.SPOT_PRICE)||140; added SPOT_PRICE=140 to wrangler.toml [vars]. Worker now matched to frontend. Also verified clean: #88 live flag, #89 demo seed (≈A$4,400), #91 bid status propagation (refreshFromApi/mergeLogos), #92 target display, #85 deposit 20% math.
2026-09-02T06:20:00Z — #81 follow-up XSS in activity-list renderer + perf: found second unescaped logo sink — renderBids() built '<img class="sl-logo" src="' + b.logo + '" alt="">' WITHOUT escapeHtml (js/app.js:377), same stored-XSS class as the spot-grid fix in cycle #18. Escaped it AND added loading="lazy" (#20). Grep confirms both img constructors (156, 377) now escape; preview.src at 518 is DOM-assigned from local file reader (safe). Also verified + checked off: #21 (single render-blocking stylesheet, necessary), #22 (CSS kept external = cacheable shared), #24 (GitHub Pages sends NO content-encoding — platform limitation, files small), #25 (SVG icon, PNG og, svg/png/jpg user logos), #32 (Escape closes modal, Tab trap exists, carousel = real focusable buttons), #36 (aria-roledescription="carousel" + labelled controls), #43 (#45 sitemap/robots correct), #46/#48/#49/#40, #56/#65/#69/(#69 bar transition is dead effect — innerHTML re-render, noted), #72/#73/#77/#78/#79.
Intentionally deferred: #18 minify (readability > bytes), #19 combine dup selectors (minor), #80 CSP (GitHub Pages limitation — no headers control).
2026-09-02T07:20:00Z — deposit-error false promise: on /deposit error the frontend ignored dep.error, fell to "Bid placed! Deposit link will be emailed to you." — no email code exists and the deposit never happened. Now truthful: shows the Stripe error and tells the user to reopen the spot to retry payment (bid is already stored pending). Consistent with the #87 copy-honesty standard. Also verified og-image.png/favicon.svg/style.css all serve 200 (og:image actually resolves) and live copy matches the latest commit.
2026-09-02T08:20:00Z — link + endpoint integrity sweep: all internal hrefs resolve (anchor ids faq/how/specs/spots/get/main present on index; cross-page index.html#* anchors valid; press/terms/privacy/marketplace/404 all linked correctly). ownerLink href="#" is populated at runtime from DATA.ownerLink (x.com/vanshuETH) — not a dead link. Live endpoints: /history 200, /admin/bids 401 without auth, /logo/A1 404, unknown path 404, /waitlist GET 404 (POST-only — correct), 404.html serves 200. All clean.
2026-09-02T09:20:00Z — refund-copy consistency pass: found index.html:367 modal-note fallback still said "Refunded in full if you're outbid; counts toward your total if you win" (pre-JS fallback of the element app.js:529 rewrites), while FAQ/terms/JS all say "the founder refunds it in full by hand" since cycle #25. Aligned HTML fallback to the founder-refund wording so content is consistent even before JS runs. Sweep confirms zero stale "Refunded in full if"/"emailed to you" phrases remain; 4 founder-refund mentions across index/terms/app.js.
2026-09-02T10:20:00Z — deposit-confirmation race: the ?deposit=ok handler wrote "✓ Deposit received — your bid is now live." into #ticker, then called refreshFromApi() which immediately re-renders renderTicker() and OVERWROTE the confirmation — a real user returning from Stripe after paying rarely saw it. Fixed with a stickyTickerUntil window (8s) that renderTicker respects, so the confirmation survives the next polls. Also escapeHtml'd spotId and res.error in that message. Scope verified (single outer IIFE). Syntax OK.
2026-09-02T11:20:00Z — config/numbers consistency sweep: device copy fully consistent across all 6 pages (16" M5 Pro, 64GB/2TB, Silver, 18/20/16-core — no stale M4/M5-Max/36GB/old-price refs). Numbers coherent: retail A$7,049 (HTML static == DATA.goal.retail, JS rewrite identical), gross-to-raise A$10,443 (31×337 = retail÷0.675), floor A$3,740 < base grid A$4,340. Apple disclaimer present. No code change — verification pass.
2026-09-02T12:20:00Z — bid-agree UX: the #bidSubmit button was enabled even before the user checked "I agree to Terms & Privacy"; checking the box didn't change it and a click fired a blocking alert. Now the button starts disabled and only enables once the agree checkbox is checked (delegated change listener + re-disable on modal open); the old JS alert stays as belt-and-suspenders. Added .btn:disabled visual (opacity/gray/cursor). Syntax + braces OK.
2026-09-02T13:20:00Z — live-polling + logo-cache review: refreshFromApi poll (30s) uses a signature diff (spotId:amount) to skip re-render when nothing changed; fetches logos only for uncached hasLogo spots in parallel via mergeLogos, caches in logoCache, merges back. No race, no cross-spot leak, no redundant fetch-after-cache. Reset demo guard intact (hidden when live; seed+bound to saved state). No issue found — verification pass.
2026-09-02T14:20:00Z — bid-form validation review: preset chips derive from minBid (filter >=min, dedupe, cap 4) so never suggest an invalid bid; submit validates sponsor/agree/amount(email-regex, converts to AUD via toAud, rejects <minBid)/email/url(logs "company.com"->https)/logo presence. Min-bid rule matches server (worker enforces SPOT_PRICE||140 + current+10) = defense in depth after cycle #33. URL normalization + focus trap verified. No issue found — verification pass.
2026-09-02T15:20:00Z — #input-validation hardening: spotId was never validated on the server. It was interpolated raw into Stripe success_url/cancel_url query strings, the checkout product name, and DB rows — a crafted client could bid on non-existent spots (e.g. A99), create phantom history, and inject long/control chars into receipts. Fixed: spotId now validated against the real grid (/^[A-E][1-8]$/, matches the 31-spot A1–E8 layout) in ALL FOUR endpoints — /bid, /confirm, /logo/, /deposit; sponsor length-capped at 80 and URL at 200 in /bid; sponsor at 80 and bidderId at 64 in /deposit. Bound SQL params already prevented injection; this closes the input-shape/URL-tampering gap. Worker ESM syntax OK.
2026-09-02T16:20:00Z — ⚠️ CRITICAL DEPLOY GAP: verified LIVE worker is NOT running recent fixes. POST /bid with phantom spot A99 returned 200 {"ok":true,status:"pending"} — proof the spot-validation (cycle #43) AND the SPOT_PRICE min-bid fix (cycle #33) are NOT live. Last worker deploy was v10 commit 75b8ce8; all worker changes since (deposit/refund truthy copy, SPOT_PRICE, spot/grid validation, length caps) are committed but undeployed. Root cause: no CLOUDFLARE_API_TOKEN in env and wrangler not authenticated in this environment — deploy requires founder's Cloudflare creds. Live worker currently ACCEPTS phantom spots and under-list-price bids. Escalated to founder for npx wrangler deploy.
2026-09-02T17:20:00Z — carousel swipe FIX (touch): the swipe handler bound the SAME down/up state machine to BOTH touchstart/touchend AND pointerdown/pointerup. On touch devices a swipe fires both event types, so touchstart reset startX/Y after pointerdown and pointerup measured dx≈0 against stale coords → horizontal touch swipes frequently failed to register (desktop mouse worked since pointer-only). Rewrote to pure Pointer Events (touch+mouse+pen uniform), moved detection to pointermove/up delta, pointermove non-passive to suppress the competing page scroll. Copy promised "swipe" so this was a real broken feature on mobile. Frontend verified live+current (all cycles #30-41 on GitHub Pages; only the Worker backend is deploy-blocked).
2026-09-02T18:20:00Z — post-close UX: after the auction closes (DATA.closing, 2026-09-30) the countdown said "closed" but clicking a spot still opened the full bid modal — a user could fill name/email/logo/amount and only hit a server 403 (or a confusing client alert). Added a guard in openModal: after close it no longer opens the modal, shows a clear ticker message ("This auction has closed — no more bids. The final look is logged below.") with an 8s sticky window. Uses the same DATA.closing source as the countdown. Syntax OK. This is a near-close UX that matters since close is 28 days out.
2026-09-02T19:20:00Z — currency + localStorage review: CCY_KEY persists AUD/USD via store (safe try/catch wrapper when localStorage blocked); setCcy re-renders all views, clears the amount field, keeps stored bids strictly in AUD (display-only conversion via fmt/fmtVal/toAud — no data-corruption path). USD view rounds converted values with en-US locale; AUD with en-AU. Verified clean — no conversion bug, no persistence leak, single source of truth for both ccy and theme keys. Frontend remains current on GitHub Pages; Worker deploy gap still open (blocked on founder, retested A99→200).
2026-09-02T20:20:00Z — theme + anchor + structure verification: theme toggle uses blt_theme_v1 as single source (head pre-paint + app.js set/sync — no desync, cycle #67 intact); site-header is NOT fixed/sticky so native #spots/#how anchors land without offset; all 6 pages re-checked for duplicate IDs and <div> balance after recent edits — all clean. No code change; frontend current on Pages; Worker deploy gap still open (blocked).
2026-09-02T21:20:00Z — storage hardening: client enforces LOGO_MAX 256KB but the server had NO logo size cap — a crafted client could POST multi-MB base64 directly to /bid, bloating D1 logo_data. Added server cap (logo.length <= 400000, covers legit 256KB base64 ~349,525 chars incl. data-URI + margin) in /bid. Rounding out cycle #43's sponsor/url caps. Committed; still blocked on the Worker deploy (all server fixes pending founder's wrangler deploy).
